spring:
  profiles:
    active: local
  application:
    name: backend
  config:
    import: "optional:file:env.properties"
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://host.docker.internal:5400/commonground}
    username: ${POSTGRES_USER:commonGroundUser}
    password: ${POSTGRES_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true
#    locations: classpath:db/migration

logging:
  level:
    org.flywaydb: INFO

