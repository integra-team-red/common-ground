services:
  db:
    image: postgres:latest
    container_name: common_ground_postgres
    env_file: "env.properties"
    restart: unless-stopped
    ports:
      - "5400:5432"
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
    profiles: [backend]
