name: ArchUnit Tests 🏛️

on: [pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  archunit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources 📄
        uses: actions/checkout@v6

      - name: Setup Java ☕
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: 24

      - name: Setup Gradle 💾
        uses: gradle/actions/setup-gradle@v4

      - name: Run ArchUnit tests 🏛️
        run: ./backend/gradlew -p ./backend/ test --tests '*VerticalSliceArchitectureTest'
